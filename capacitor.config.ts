import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.laporbosqu.app',
  appName: 'laporbosquapps',
  webDir: 'dist/spa',
  bundledWebRuntime: false
};

export default config;
